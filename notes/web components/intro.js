/**
 * 
 * can use components to create applications similar to react
 * 
 * those elements become accessible from any other styles or js files you have loading on the page
 * if you don't want this to happen have to use something called the shadow dom, will encapsulate everything inside the component
 * 
 */