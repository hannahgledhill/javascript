/**
 * note - you need to rerun parcel if you add new files and refresh the browser
 * 
 * can even use scss and parcel will change it to css???
 * 
 * if get error with spread operator [...array] might need to install some babel plugin
 * 
 * can use with react-router-dom and just have <Router>links links links and route route route</Router> on app component and then in components can also use <link> within the component to link to other pages again
 */